import React from 'react';
import Layout from '../core/Layout';

// This page should be restricted to authenticated Admin User ONLY
const Admin = () => (
    <Layout>
        <h1>Admin Page</h1>
    </Layout>
)

export default Admin;